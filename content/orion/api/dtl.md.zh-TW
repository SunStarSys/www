---
archived: ~
categories: ~
dependencies: '*.md.zh-TW'
keywords: 休息，API
published: ~
status: 草稿
title: Orion API - Django 範本庫
---

{# lede #}本文件涵蓋 **Django 範本程式庫 (DTL) ** API{# lede #}

[TOC]

----

## DTL (Django 1.0 範本庫).

### Django 1.0 過濾器延伸 ([Dotiac：：DTL：：過濾](https://github.com/SunStarSys/orion/blob/master/lib/Dotiac/DTL/Filter.pm).

#### 附加

採用單一引數，並將其值附加至篩選的輸入。引數可以是 varname 或引號字串；此過濾器將會更聰明`/` 結合在一起的字元[`附加`](#).

#### 個剪下

類似[`切割`](#).

#### LED

擷取介於之間的文字&#123;# [`鉛`](#) #&#125;

#### ssi

遞迴評估所有 Django [`ssi`](#).

#### starts_with

前置碼比對的測試。

#### 目錄名稱

傳回傳統 UNIX [`目錄名稱`](#).

#### parse_filename

介面[`SunStarSys::Util::parse_filename`](#)繁體中文關鍵差別在於該次常式的前兩個傳回值已交換，且路徑延伸全部中斷 (含) [`.`](#) prefixed) 進入個別引數，讓此篩選將代表索引清單的引數字串帶入產生的陣列中。作為特殊情況，引數的結尾是[`..`](#).

#### 基礎名稱

傳回傳統 UNIX [`基本名稱`](#) 篩選輸入字串中的路徑。傳送以下項目的引數[`0`](#).

#### tex2md

轉換$$\LaTeX$$ 進入 Markdown+$$\KaTeX$$

#### md2tex

逆變換`tex2md`

#### vcs_date

接受[`仰光`](#) 提供地區設定特定表示法的引數，該引數代表最近所呈現的變更之日、月及年[`$Date`](#) 篩選輸入字串中的 Subversion 關鍵字 (通常是完整的) [`內容`](#).

#### vcs_time

代表最近所呈現變更的小時、分鐘、秒以及時區位移[`$Date`](#).

#### vcs_author

顯示[`安全`](#) 以 HTML 表示最近更新內容的使用者，如 Subversion 關鍵字所記錄[`$ 作者`](#) 篩選輸入字串中的關鍵字。選擇性[`仰光`](#).

#### vcs_revision

顯示最近對內容所做變更的數值修訂版本編號，由此記錄[`$ 修訂`](#).

#### strip_prefix

從篩選的輸入字串 (路徑) 中去除 (path) 前置碼 (作為此篩選的引數)。前置碼預設為正規表示式[`\S+/content/`](#).

#### 打亂

打亂陣列 。

#### img

從篩選的內容擷取第一個 HTML5/Markdown 影像。

<!-- $Date$ $Author$ $Revision$ -->
