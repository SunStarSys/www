---
categories: ~
dependencies: '*.md.zh-TW'
keywords: 休息，API
status: 驗證 =34604
title: Orion API - 建置
---

{# lede #}本文件涵蓋 **組建系統** API{# lede #}.

基本上，建置系統由兩個使用者提供的 Perl 模組所管理：[`程式庫 /path.pm`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/path.pm) 與[`程式庫 /view.pm`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/view.pm).

前的工作是做三件事：

0. 載入[`程式庫 /facts.yml`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/facts.yml) 與[`程式庫 /acl.yml`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/acl.yml),
1. 建構`@path：：樣式`，和
2. 當機行走`內容 /` 種樹`%path::dependencies` 與`@path::acls` 來自 Markdown/yaml 檔案標頭描述資料。

後者的工作是提供不可呼叫的工作`檢視`- 基底`$ 方法`s，用於比對下列項目：`@path：：樣式` (作為每個 arrayref 項目的第二個槽位中的字串方法名稱)，由[建置命令檔](https://github.com/SunStarSys/orion/blob/master/build_site.pl#L219-L258) 下方 ...

```perl
#api
  ...

my $path = "/content-rooted/path/to/source/file";

for my $p (@path::patterns) {
    my ($re, $method, $args) = @$p;
    next unless $path =~ $re;
    ++$matched;

my ($content, $mime_extension, $final_args, @new_sources) = view->can($method)->(path => $path, lang => $lang, %$args);

... write UTF-8 decoded $content to target file with associated $mime_extension file-type, and feed @new_sources back into the build.
  }

copy_if_newer("content$path", "$ENV{TARGET}/content$path") unless $matched;

...
#api
```

許多視觀表的堆疊方式"篩選" 預先處理檔案的層面`$path` 新穎，如外部程式碼`片段` 或`漸近體`-fenced markdown 區塊。您可以看到以下範例：[這裡](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/path.pm#L53).

[TOC]

----

## 建立系統

### [SunStarSys：：查看](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/View.pm) &mdash; 基本類別`程式庫 /view.pm`

#### single_narrative (%args) &mdash; 最受歡迎 (和最複雜的) 檢視

此檢視包含檔案自動處理 (位於`$path`的附件目錄。也就是說，如果`$path = "繁體中文"`，然後儲存於 中的檔案`/foo.page/` 與目錄關聯的目錄".en" 語言延伸將併入該範本可定址引數`$path` 無論`前處理` 引數設定&mdash; 如果為 true，也會讓 **頁面的內容本身能夠存取該資料 **。

必要引數：

- [`範本`](#)
- [`路徑`](#)
- [`語言`](#)

選擇性引數：

- [`描述`](#) &mdash; 置換正常[`fetch_deps`]() 處理

- [`quick_deps`](#) &mdash; 內部折舊處理最佳化設定；最佳剩餘取消設定

- [`前處理`](#) &mdash; 啟用範本處理[`$path`](#) 內容本身，

- [`archive_root`](#) &mdash; 檔案位於"已封存" 狀態為"已複製" 並透過下列方式依年 / 月子資料夾追蹤至此內容根目錄的位置：`ssi`,

- [`category_root`](#) &mdash; 項目在"分類" 標頭為"已複製" 覆寫為內容根目錄位置中適當命名的類別資料夾`ssi`.

#### news_page (%args) &mdash; 用於多敘述彙總頁面

#### 網站地圖 (%args) &mdash; 用於建立 index.html 和 sitemap.html 頁面

語言環境特定、已排序的相依性索引。

必要引數：

- [`路徑`](#)
- [`語言`](#)

選擇性引數：

- [`quick_deps`](#)
- [`巢套`](#)
- [`前處理`](#)

#### asymptote (%args)

組建和快取[`漸近線`](https://asymptote.sourceforge.io/) 適用於啟用 HTML5-WebGL 畫面之向量圖形的三重引號編碼區塊。

必要引數：

- [`檢視`](#)
- [`語言`](#)
- [`路徑`](#)

#### 略過 (%args)

根本不要建置這些元件。改為建立關聯的產生來源檔案 (例如，`.bib\$lang` $$\mapsto$$ `\$base.page/bibliography.yml\ 語言`) 將建立在次要建構系統執行上。

#### yml2ext (%args)

將 YAML 檔案轉換成 JSON。

選擇性引數：

- [`分機`](#) 預設為`json`
- [`過濾器`](#) 預設為`json_raw`
- [`範本`](#) 覆寫`過濾器` 預設表示式

#### fetch_deps ($path, $data, $quick)

將 $data 引數雜湊參照重製為 2 元素陣列參照的時間戳記排序陣列參照。每個 2- 元素 arrayref 中的第一個項目是檔案 path-name，第二個元素則是產生的[`read_text_file`](#) 該 path-name 的 hashref。

傳回結果新來源檔案的清單，如果[`$quick > 2`](#).

必要引數：

- [`路徑`](#)
- [`資料`](#) - 輸入為雜湊參考；在退回時導致折舊陣列的商店
- [`快速`](#) - 預設為 2

#### 來源路徑 ($path)

傳回下列項目的 HTML 來源路徑清單：[$path](#).

#### memoize (%args)

快取建置；主要與 fetch_deps 和 quick_deps > 2 搭配使用。

#### 註解 (%args)

產生頁面註解的 SSI 引入的 HTML 片段。

#### next_view (%args)

處理 $args 的公用程式{檢視}.

#### ssi (%args)

遞迴評估[ssi](#) 標籤。

#### 離線 (%args)

以離線模式執行 next_view。

#### 程式碼片段 (%args)

[處理外部代碼](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/Value/Snippet.pm#L12-L13) [片段線](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/View.pm#L806)，通常會將其 GitHub 上的來源位置匯入程式設計語言特定的圍欄標記區塊。[這裡範例](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/content/joe/perl7-sealed-lexicals.md.en#L135).

#### 重新建構 (%args)

從 next_view 重新處理內建內容中的樣板指令。

#### trim_local_links (%args)

去除本機連結的副檔名。

#### normalize_links (%args)

正規化本地端連結 (. / 與 .. /) 。

----

### [SunStarSys：：使用](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/Util.pm) &mdash; 公用程式程式庫[`程式庫 /path.pm`](#) 與[`程式庫 /view.pm`](#)

#### read_text_file ($file, $out, $content_lines) &mdash; Orion 的通用文字檔案處理器

剖析 UTF- 8 編碼檔案的 headers+content [`$ 檔案`](#) 並將結果儲存於[`折扣`](#). [`$content_lines`](#) 是要讀取的內容行數上限 (選擇性)。
傳回實際讀取的行數 (包含標頭) 。

[`$ 檔案`](#) 可以是原始字串的參照，代表檔案的完整內容。結果[`折扣`](#) 將會依然以 UTF-8 編碼。

#### copy_if_newer ($src, $dest)

份數[`$src`](#) 至[`$dest`](#) 如果前者的修改時戳比後者的修改時戳新。在複製時，加上 gzip 壓縮[`$dest`](#) 檔案 (如果是文字檔) 並新增".gz" 名稱的副檔名。

#### get_lock ($lockfile)

獨佔 (f) 鎖定 (針對目前的 UNIX 處理作業) 於[`$lockfile`](#).

#### 打亂 (\\)&#64;版面)

就地隨機 (漁船) 混亂[`@deck`](#).

#### sort_tables ($content)

根據每個表格的資料欄規格，在 $content 中排序 Markdown 表格。每一表格只能排序一個資料欄 (選擇性地為數字) [`n 個`](#)，遞減[`垂直`](#) 或遞增[`^`](#) 訂單。

#### fixup_code ($prefix, $type, &#64;_)

刪除每一個參數的 $prefix &#64;\_. $type 引數的函數是實行特定的函數，但主要是用來植入 editor.md "模式" 處理此內容於&#64;_.

#### unload_package ($pkg)

積極卸載 Perl (分葉) 套裝程式[`$pkg`](#) 來自符號表 (STASH)。

#### purge_from_inc(&#64;路徑)

移除[`@paths`](#) 來自[`@INC`](#).

#### 中文 (繁體)&#64;_)

觸碰所有檔案於[`@_`](#)。如果未傳送任何引數，則使用[`$_`](#).

#### normalize_svn_path(&#64;_)

將所有路徑標準化[`@_`](#) 以安全方式用作原始引數[`SVN：：客戶端`](#) 指令。

#### sanitize_relative_path(&#64;_)

保護以下位置的路徑：[`@_`](#) 用作純相對路徑[`多蒂亞克：：DTL`](#) (Django 樣板) 路徑特定命令。

#### parse_filename (路徑)

包裝函式四周[`檔案：：Basename：：fileparse`](#)。不含引數，使用[`$_`](#) 作為要剖析的檔案名稱。

#### walk_content_tree ($code)

有條件地步行[`。/ 內容`](#) 組建系統取出樹狀結構，先標準化[`$_`](#) 作為正式的內容根目錄子路徑，然後呼叫[`中文 (繁體)`](#) 在樹行道的每個項目上。對大多數的組建而言，一律不會執行 walk &mdash; 而是組建依賴先前組建的快取資料。

強制散步的唯一方法是設定[`$path::use_cache`](#) 在使用者提供的模組中為 false 值。否則，此行為由下列項目由專家管理：[增量建置技術](https://iconoclasts.blog/joe/dependencies).

如果實際繼續執行 walk，則傳回 1，而不是依賴快取的資料。否則會傳回假值。

##### 已歸檔 ($path)

每個旗標[`狀態：已存檔`](#) [`$path`](#) (以自然語言特定的方式)。使用[`$_`](#) 如果未傳送任何引數。

歸檔檔案是告訴 Orion 的自然方式"停止注意此檔案的永久連結位置 ... 除非再次更新，否則會重新整理存檔位置。特別是，封存的檔案不會出現在 CMS 本身的目錄列表中；您必須瀏覽至即時頁面本身，才能在線上再次進行編輯。

##### seed_file_deps (路徑)

安全更新[`%path::dependencies`](#) 針對此[`$path`](#)，根據它[`相依性`](#) 標頭 glob。預設使用[`$_`](#) 作為路徑，若未傳送任何引數。

##### seed_file_acl (路徑)

安全更新[`@path::acl`](#) 針對此[`$path`](#)，根據它[`存取控制清單`](#) 標頭規格預設使用[`$_`](#) 作為路徑，若未傳送任何引數。

#### 載入

相同於[`YAML::XS::Load`](#).

#### 傾印

相同於[`YAML：：XS：：傾印`](#).

<!-- $Date$ $Author$ $Revision$ -->
