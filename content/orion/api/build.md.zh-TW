---
categories: ~
dependencies: '*.md.zh-TW'
keywords: 休息，API
status: 驗證 =44187
title: Orion API - 建置
---

<div class="right">

![SunStar 系統](../../images/sunstarstaronly)

</div>
<ul class="nav nav-tabs" role="tablist">
  <li class="nav-item" role="presentation">
    <a class="nav-link" data-bs-toggle="tab" href="#stats" aria-selected="false" role="tab" tabindex="-1">統計</a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link active" data-bs-toggle="tab" href="#docs" aria-selected="true" role="tab">文件</a>
  </li>
</ul>
</div>
<div class="tab-content">
<div class="tab-pane fade active show" id="docs" role="tabpanel">

&nbsp;

[TOC]#sidebar

## 建立系統

{# lede #}本文件涵蓋 **組建系統** API{# lede #}.

基本上，建置系統由兩個使用者提供的 Perl 模組所管理：[`lib/path.pm`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/path.pm) 與[`lib/view.pm`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/view.pm).

前的工作是做三件事：

0. 載入[`lib/facts.yml`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/facts.yml) 與[`lib/acl.yml`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/acl.yml),
1. 建構`@path::patterns`，和
2. 當機行走`content/` 種樹`%path::dependencies` 與`@path::acls` 來自 Markdown/yaml 檔案標頭描述資料。

後者的工作是提供不可呼叫的工作`view`- 基底`$method`s，用於比對下列項目：`@path::patterns` (作為每個 arrayref 項目的第二個槽位中的字串方法名稱)，由[建置命令檔]({{snippetA.pretty_uri}}):

[snippet:repo=SunStarSys/orion:path=build_site.pl:token=#api:lang=perl]

&nbsp;

許多視觀表的堆疊方式"篩選" 以預先處理 (或後續處理) 檔案的層面`$path` 新穎，如外部程式碼`snippets` 或`asymptote`-fenced markdown 區塊。您可以看到以下範例：[這裡](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/path.pm#L52).

&nbsp;

----

&nbsp;

### [`SunStarSys::View`](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/View.pm) &mdash; 基本類別`lib/view.pm`

&nbsp;

<div class="card border-primary mb-3">
  <div class="card-header">

#### `single_narrative(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

最受歡迎 (和最複雜的) 檢視

</div>

<p class="card-text">

此檢視包含檔案自動處理 (位於`$path`的附件目錄。也就是說，如果`$path = "/foo.md.en"`，然後儲存於 中的檔案`/foo.page/` 與目錄關聯的目錄".en" 語言延伸將併入該範本可定址引數`$path` 無論`preprocess` 引數設定&mdash; 如果為 true，也會讓 **頁面的內容本身能夠存取該資料 **。

Mandatory Arguments:

- `template`
- `path`
- `lang`

Optional Argmuents:

- `deps` &mdash; 置換正常`fetch_deps` 處理

- `quick_deps` &mdash; 內部折舊處理最佳化設定；最佳剩餘取消設定

- `preprocess` &mdash; 啟用範本處理`$path` 內容本身，

- `archive_root` &mdash; 檔案位於"已封存" 狀態為"已複製" 並透過下列方式依年 / 月子資料夾追蹤至此內容根目錄的位置：`ssi`,

- `category_root` &mdash; 項目在"分類" 標頭為"已複製" 覆寫為內容根目錄位置中適當命名的類別資料夾`ssi`.

</p>
  </div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `news_page(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

針對 (多敘述) 彙總頁面

</div>

<p class="card-text">

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `sitemap(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

用於建立 index.html 和 sitemap.html 頁面

</div>

<p class="card-text">

語言環境特定、已排序的相依性索引。

Mandatory Arguments:

- `path`
- `lang`

Optional Arguments:

- `quick_deps`
- `nested`
- `preprocess`

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `asymptote(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

組建和快取[`Asymptote`](https://asymptote.sourceforge.io/) 適用於啟用 HTML5-WebGL 畫面之向量圖形的三重方括號編碼區塊

</div>
<p class="card-text">

Mandatory Arguments:

- `view`
- `lang`
- `path`

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `skip(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

根本不要建置這些元件。

</div>
<p class="card-text">

改為建立關聯的產生來源檔案 (例如，`.bib\$lang` $$\mapsto$$ `\$base.page/bibliography.yml\$lang`) 將建立在次要建構系統執行上。

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `yml2ext(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

將 YAML 檔案轉換成 JSON。

</div>
<p class="card-text">

Optional Arguments:

- `ext` 預設為`json`
- `filter` 預設為`json_raw`
- `template` 覆寫`filter` 預設表示式

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `csv2ext(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

將 CSV 檔案轉換成 JSON。

</div>
<p class="card-text">

Optional Arguments:

- `ext` 預設為`json`
- `filter` 預設為`json_raw`
- `template` 覆寫`filter` 預設表示式

</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

#### `fetch_deps($path, $data, $quick)`

</div>
  <div class="card-body">
    <div class="card-title">

重構`$data` 引數 hashref 做為 2-element arrayrefs 的時戳排序 arrayref。

</div>
<p class="card-text">

每個 2- 元素 arrayref 中的第一個項目是檔案 path-name，第二個元素則是產生的`read_text_file` 該 path-name 的 hashref。

傳回結果新來源檔案的清單，如果`$quick > 2`.

Mandatory Arguments:

- `path`
- `data` - 輸入為 hashref；儲存傳回時產生的 dep 陣列參考資料
- `quick` - 預設為 2

</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

#### `breadcrumbs($path)`

</div>
  <div class="card-body">
    <div class="card-title">

傳回下列項目的 HTML 來源路徑清單：`$path`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `memoize(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

快取建置；主要與`fetch_deps` 與`quick_deps > 2`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `comment(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

產生`SSI`- 包含`HTML5` 頁面註解的片段。

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

#### `next_view(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

循序處理的公用程式`$args{view}`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `ssi(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

遞迴評估`ssi` 標籤。

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `offline(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

執行`next_view` 在離線模式。

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `snippet(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

[處理外部代碼](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/Value/Snippet.pm#L12-L13) [片段線](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/View.pm#L806)，通常會將其 GitHub 上的來源位置匯入程式設計語言特定的圍欄標記區塊。[這裡範例](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/content/joe/perl7-sealed-lexicals.md.en#L135).

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `reconstruct(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

重新處理`Template` 內建內容的指令來源`next_view`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `trim_local_links(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

去除本機連結的副檔名。

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `normalize_links(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

標準化本機連結 (`./` 與`../`).

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `langify_template(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

附加`$args{lang}` 至`$args{template}`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `titleize_links(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

新增標題字串至 Markdown 連結。一般使用於`offline` 檢視以避免從遠端網站提取標題，這會大幅拖放頁面建置時間。

</div>
<p class="card-text">
</p>
</div>
</div>

&nbsp;

----

### [`SunStarSys::Util`](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/Util.pm) &mdash; 公用程式程式庫`lib/path.pm` 與`lib/view.pm`

&nbsp;

<div class="card border-primary mb-3">
  <div class="card-header">

#### `read_text_file($file, $out, $content_lines)`

</div>
  <div class="card-body">
    <div class="card-title">

Orion 的通用文字檔案處理器

</div>
<p class="card-text">

剖析 UTF- 8 編碼檔案的 headers+content `$file` 並將結果儲存在 hashref 中`$out`. `$content_lines` 是要讀取的內容行數上限 (選擇性)。

傳回實際讀取的行數 (包含標頭) 。

`$file` 可以是原始字串的參照，代表檔案的完整內容。結果`$out` 將會依然以 UTF-8 編碼。

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `copy_if_newer($src, $dest)`

</div>
  <div class="card-body">
    <div class="card-title">

份數`$src` 至`$dest` 如果前者的修改時戳比後者的修改時戳新。

</div>
<p class="card-text">

在複製時，加上 gzip 壓縮`$dest` 檔案 (如果是文字檔) 並新增".gz" 名稱的副檔名。`GZIP` 設定可停用壓縮`$ENV{NO_COPY_COMPRESS}=1`.

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `get_lock($lockfile)`

</div>
  <div class="card-body">
    <div class="card-title">

獨佔 (f) 鎖定 (針對目前的 UNIX 處理作業) 於`$lockfile`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `shuffle(\@deck)`

</div>
  <div class="card-body">
    <div class="card-title">

就地隨機 (漁船) 混亂`@deck`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `sort_tables($content)`

</div>
  <div class="card-body">
    <div class="card-title">

排序減價表格`$content` 根據每個表格的資料欄規格。

</div>
<p class="card-text">

每一表格只能排序一個資料欄 (選擇性地為數字) `n`，遞減`v` 或遞增`^` 訂單。

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `fixup_code($prefix, $type, @_)`

</div>
  <div class="card-body">
    <div class="card-title">

分割`$prefix` 來自每個引數&#64;\_.

</div>
<p class="card-text">

目的：`$type` 引數是實行特定的，但主要是用來植入`editor.md "mode"` 處理此內容於&#64;_.

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `unload_package($pkg)`

</div>
  <div class="card-body">
    <div class="card-title">

積極卸載`Perl` (分葉) 封裝`$pkg` 來自符號表 (`STASH`).

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `purge_from_inc(@paths)`

</div>
  <div class="card-body">
    <div class="card-title">

移除`@paths` 來自`@INC`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `touch(@_)`

</div>
  <div class="card-body">
    <div class="card-title">

觸碰所有檔案於`@_`。如果未傳送任何引數，則使用`$_`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `normalize_svn_path(@_)`

</div>
  <div class="card-body">
    <div class="card-title">

將所有路徑標準化`@_` 以安全方式用作原始引數`SVN::Client` 指令。

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `sanitize_relative_path(@_)`

</div>
  <div class="card-body">
    <div class="card-title">

保護以下位置的路徑：`@_` 用作純相對路徑`Dotiac::DTL` (Django 樣板) 路徑特定命令。

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `parse_filename($path)`

</div>
  <div class="card-body">
    <div class="card-title">

包裝函式四周`File::Basename::fileparse`。不含引數，使用`$_` 作為要剖析的檔案名稱。

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `walk_content_tree($code)`

</div>
  <div class="card-body">
    <div class="card-title">

有條件地步行`./content` 組建系統取出樹狀結構；先標準化`$_` 作為正式的內容根目錄子路徑，然後呼叫`$code->()` 在樹行道的每個項目上。

</div>
<p class="text-body">

對大多數的組建而言，一律不會執行 walk &mdash; 而是組建依賴先前組建的快取資料。

強制散步的唯一方法是設定`$path::use_cache` 在使用者提供的模組中為 false 值。否則，此行為由下列項目由專家管理：[增量建置技術](https://iconoclasts.blog/joe/dependencies).

如果實際繼續執行 walk，則傳回 1，而不是依賴快取的資料。否則會傳回假值。

</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

##### `archived($path)`

</div>
  <div class="card-body">
    <div class="card-title">

每個旗標`Status: archived` `$path` (以自然語言特定的方式)。使用`$_` 如果未傳送任何引數。

</div>
<p class="card-text">

封存 Markdown 檔案是告訴 Orion 的自然方法"停止注意此檔案的永久連結位置"，除非再次更新，否則會重新整理存檔位置。特別是，封存的檔案不會出現在 Orion CMS 本身的目錄清單中；您必須瀏覽至即時頁面本身，才能在線上再次進行編輯。

</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

##### `seed_file_deps($path)`

</div>
  <div class="card-body">
    <div class="card-title">

安全更新`%path::dependencies` 針對此`$path`，根據它`dependencies` 標頭`glob(s)`.

</div>
<p class="card-text">

預設使用`$_` 作為路徑，若未傳送任何引數。

</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

##### `seed_file_acl($path)`

</div>
  <div class="card-body">
    <div class="card-title">

安全更新`@path::acl` 針對此`$path`，根據它`acl` 標頭規格

</div>
<p class="card-text">

預設使用`$_` 作為路徑，若未傳送任何引數。

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `Load`

</div>
  <div class="card-body">
    <div class="card-title">

相同於`YAML::XS::Load`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `Dump`

</div>
  <div class="card-body">
    <div class="card-title">

相同於`YAML::XS::Dump`.

</div>
<p class="card-text">
</p>
</div>
</div>
  </div>

<div class="tab-pane fade" id="stats" role="tabpanel">
    <script src="/editor.md/js/chart.umd.js"></script>
    <canvas id="myChart" width="100%" height="800px"></canvas>
</div>
</div>

<script async="" type="module">
  var ctx = document.getElementById("myChart").getContext("2d");
  const response = await fetch("/dynamic/search/?regex=build%3D;lang={{lang}};as_json=1;markdown_search=1");
  if (response.ok) {
    const json = await response.json();
	const data = json.duration;
    const values = data.map(x => x[1]);
    var total = 0;
    for (var i=0; i < values.length; ++i)
        total += +values[i];
    const labels = data.map(x => "r" + x[0] + ":" + x[2] + ":" + x[3] + ":" + x[4]);
    var myChart = new Chart(
      ctx,
      {
          type: "bar",
          data: {
              labels: labels.reverse(),
              datasets: [{
                  label: "Build Duration (s)",
                  data: values.reverse(),
                  backgroundColor: "#8f99fb",
              }],
          },
          options: {
              indexAxis: "y",
              plugins: {
                  title: {
                      display: true,
                      text: (total / 60).toFixed(0) + " build minutes this month (average build duration is " + (total / values.length).toFixed(0) + " s)",
                  }
              },
              onClick: (e, elts, chart) => {
                  if (elts) {
                      const idx = elts[0].index;
                      const revision = labels[idx].split(/:/)[0];
                      document.location = "/dynamic/search/?regex=build=" + revision + ";lang={{lang}};markdown_search=1";
                  }
              },
          },
      });
  }
</script>

<!-- $Date$ $Author$ $Revision$ -->
