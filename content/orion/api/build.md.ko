---
categories: ~
dependencies: '*.md.ko'
keywords: REST, 아피
status: 확인=35135
title: Orion API - 빌드
---

<div class="right">

![SunStar 시스템](../../images/sunstarstaronly)

</div>
<ul class="nav nav-tabs" role="tablist">
  <li class="nav-item" role="presentation">
    <a class="nav-link" data-toggle="tab" href="#stats" aria-selected="false" role="tab" tabindex="-1">통계</a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link active" data-toggle="tab" href="#docs" aria-selected="true" role="tab">설명서</a>
  </li>
</ul>
</div>
<div class="tab-content">
<div class="tab-pane fade active show" id="docs" role="tabpanel">

&nbsp;

## 빌드 시스템

{# lede #}이 문서에서는 **시스템 구축** API에 대해 다룹니다.{# lede #}.

기본적으로 빌드 시스템은 사용자가 제공한 두 개의 Perl 모듈로 제어됩니다. [`lib/path.pm`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/path.pm) 및 [`lib/view.pm`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/view.pm).

이전 작업은 다음 세 가지를 수행하는 것입니다.

0. 로드 [`lib/facts.yml`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/facts.yml) 및 [`lib/acl.yml`](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/acl.yml),
1. 생성 `@path::patterns`및
2. 기회주의적으로 걷다 `content/` 시드할 트리 `%path::dependencies` 및 `@path::acls` 마크다운/yaml 파일 헤더 메타데이터에서

후자의 임무는 호출을 제공하는 것입니다 `view`- 기반 `$method`s - 일치하는 항목의 경우 `@path::patterns` (각 arrayref 항목의 두번째 슬롯에 있는 문자열화된 메소드 이름으로), [스크립트 작성]({{snippetA.pretty_uri}}):

[snippet:repo=SunStarSys/orion:path=build_site.pl:token=#api:lang=perl]

많은 뷰가 "필터" 파일의 측면을 미리 처리하려면 `$path` 외부 코드와 마찬가지로 참신합니다. `snippets` 또는 `asymptote`-fenced 마크다운 블록. 예를 들면 다음과 같습니다. [여기](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/lib/path.pm#L53).

[TOC]

&nbsp;

----

&nbsp;

### [`SunStarSys::View`](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/View.pm) &mdash; 기본 클래스 `lib/view.pm`

&nbsp;

<div class="card border-primary mb-3">
  <div class="card-header">

#### `single_narrative(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

가장 인기있는 (그리고 정교한)보기

</div>

<p class="card-text">

이 뷰에는 내부에 있는 파일의 자동 처리 기능이 포함되어 있습니다. `$path`첨부 디렉토리입니다. 즉, `$path = "/foo.md.en"`에 저장된 파일이  `/foo.page/` 디렉토리: "한국어" 언어 확장이 템플리트 주소 지정 가능 인수에 통합됩니다. `$path` 에 관계없이 `preprocess` 인수의 설정 &mdash; true인 경우 **페이지의 콘텐츠 자체**에서 해당 자료에 액세스할 수 있게 됩니다.

필수 인수:

- `template`
- `path`
- `lang`

선택적 인수:

- `deps` &mdash; 법선 재지정 `fetch_deps` 처리 중

- `quick_deps` &mdash; 내부 deps-processing 최적화 설정, 최선 설정 해제

- `preprocess` &mdash; 템플리트가 `$path` 내용 자체,

- `archive_root` &mdash; 파일 위치 "아카이브됨" 상태: "복사됨" 이 콘텐츠 루트 위치에 대한 연도/월 하위 폴더별로 추적 `ssi`,

- `category_root` &mdash; 의 항목 "범주" 헤더 "복사됨" 이 컨텐츠 루트 위치에 있는 적절하게 명명된 범주 폴더로 `ssi`.

</p>
  </div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `news_page(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

(다중 설명) 집계 페이지

</div>

<p class="card-text">

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `sitemap(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

index.html 및 sitemap.html 페이지 작성용

</div>

<p class="card-text">

종속성의 로케일별 정렬된 인덱스입니다.

필수 인수:

- `path`
- `lang`

선택적 인수:

- `quick_deps`
- `nested`
- `preprocess`

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `asymptote(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

빌드 및 캐시 [`Asymptote`](https://asymptote.sourceforge.io/) HTML5-WebGL 캔버스 사용 벡터 그래픽에 대한 삼중 백쿼트 코드 블록

</div>
<p class="card-text">

필수 인수:

- `view`
- `lang`
- `path`

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `skip(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

이것들을 전혀 만들지 마십시오.

</div>
<p class="card-text">

대신 연관된 생성된 소스 파일(예: `.bib\$lang` $$\mapsto$$ `\$base.page/bibliography.yml\$lang`)는 보조 빌드 시스템 실행에 빌드됩니다.

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `yml2ext(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

YAML 파일을 일반적으로 JSON으로 변환합니다.

</div>
<p class="card-text">

선택적 인수:

- `ext` 기본값 `json`
- `filter` 기본값 `json_raw`
- `template` 재정의 `filter` 기본 표현식

</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

#### `fetch_deps($path, $data, $quick)`

</div>
  <div class="card-body">
    <div class="card-title">

리팩터 `$data` 인수 hashref는 2-요소 arrayrefs의 시간 기록 정렬된 arrayref입니다.

</div>
<p class="card-text">

각 2 요소 arrayref의 첫 번째 항목은 파일 경로 이름입니다, 두 번째 요소는 결과입니다 [`read_text_file`](#) 해당 경로 이름에 대한 해시 참조입니다.

다음과 같은 경우 결과 새 소스 파일 목록을 반환합니다. `$quick > 2`.

필수 인수:

- `path`
- `data` - 해시 참조로 입력; 반환 시 결과 배열 참조를 저장합니다.
- `quick` - 기본값은 2입니다.

</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

#### `breadcrumbs($path)`

</div>
  <div class="card-body">
    <div class="card-title">

다음에 대한 HTML 이동 경로 목록을 반환합니다. `$path`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `memoize(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

빌드를 캐시에 저장합니다. 주로 `fetch_deps` 및 `quick_deps > 2`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `comment(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

생성 `SSI`-포함가능 `HTML5` 페이지 주석에 대한 부분입니다.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

#### `next_view(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

순차적 처리를 위한 유틸리티 `$args{view}`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `ssi(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

반복적으로 평가 `ssi` 태그

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `offline(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

다음을 실행합니다. `next_view` 오프라인 모드

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `snippet(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

[외부 코드 처리](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/Value/Snippet.pm#L12-L13) [코드 조각 행](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/View.pm#L806)일반적으로 GitHub의 소스 위치를 프로그래밍 언어별 보호된 마크다운 블록으로 가져옵니다. [예제](https://github.com/SunStarSys/www.iconoclasts.blog/blob/trunk/content/joe/perl7-sealed-lexicals.md.en#L135).

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `reconstruct(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

재처리 `Template` 내장 컨텐트의 지시어 `next_view`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `trim_local_links(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

로컬 링크에서 파일 확장자를 자릅니다.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `normalize_links(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

로컬 링크 정규화(`./` 및 `../`).

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `langify_template(%args)`

</div>
  <div class="card-body">
    <div class="card-title">

추가 `$args{lang}` 종료 `$args{template}`.

</div>
<p class="card-text">
</p>
</div>
</div>

&nbsp;

----

### [`SunStarSys::Util`](https://github.com/SunStarSys/orion/blob/master/lib/SunStarSys/Util.pm) &mdash; 유틸리티 라이브러리 `lib/path.pm` 및 `lib/view.pm`

&nbsp;

<div class="card border-primary mb-3">
  <div class="card-header">

#### `read_text_file($file, $out, $content_lines)`

</div>
  <div class="card-body">
    <div class="card-title">

Orion의 범용 텍스트 파일 프로세서

</div>
<p class="card-text">

UTF-8 인코딩 파일의 헤더+내용을 구문 분석합니다. `$file` 결과를 해시 참조에 저장합니다. `$out`. `$content_lines` 읽을 콘텐츠 라인의 최대 개수(선택 사항)입니다.

읽은 실제 행 수(머리글 포함)를 반환합니다.

`$file` 파일의 전체 내용을 나타내는 원시 문자열에 대한 참조일 수 있습니다.  결과는 `$out` UTF-8이 인코딩됩니다.

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `copy_if_newer($src, $dest)`

</div>
  <div class="card-body">
    <div class="card-title">

매수 `$src` 종료 `$dest` 이전 수정 타임스탬프가 후자의 수정 타임스탬프보다 최신인 경우.

</div>
<p class="card-text">

복사본에서 gzip-compresses의 `$dest` 파일(텍스트 파일인 경우) ".gz" 이름을 확장합니다.

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `get_lock($lockfile)`

</div>
  <div class="card-body">
    <div class="card-title">

현재 UNIX 프로세스의 경우 배타적(f)잠금을 `$lockfile`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `shuffle(\@deck)`

</div>
  <div class="card-body">
    <div class="card-title">

내장 무작위(어류) 셔플 `@deck`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `sort_tables($content)`

</div>
  <div class="card-body">
    <div class="card-title">

마크다운 테이블 정렬 위치 `$content` 각 테이블의  열 사양에 따라 다릅니다.

</div>
<p class="card-text">

테이블당 정확히 하나의 열을 정렬할 수 있습니다(선택 사항). `n`, 내림차순 `v` 또는 오름차순 `^` 주문.

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `fixup_code($prefix, $type, @_)`

</div>
  <div class="card-body">
    <div class="card-title">

스트립 `$prefix` 모든 인수에서 &#64;\_.

</div>
<p class="card-text">

의 목적 The purpose of the `$type` 인수는 구현에 따라 다르지만 주로 초기 설정에 사용됩니다. `editor.md "mode"` 이 콘텐츠를 처리하려면 &#64;_.

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `unload_package($pkg)`

</div>
  <div class="card-body">
    <div class="card-title">

적극적인 언로드 `Perl` (리프) 패키지 `$pkg` 기호 테이블(`STASH`).

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `purge_from_inc(@paths)`

</div>
  <div class="card-body">
    <div class="card-title">

제거 `@paths` 시작일 `@INC`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `touch(@_)`

</div>
  <div class="card-body">
    <div class="card-title">

모든 파일을 `@_`. 인수가 전달되지 않으면 `$_`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `normalize_svn_path(@_)`

</div>
  <div class="card-body">
    <div class="card-title">

의 모든 경로를 정규화합니다. `@_` 원시 인수로 안전하게 사용 `SVN::Client` 명령

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `sanitize_relative_path(@_)`

</div>
  <div class="card-body">
    <div class="card-title">

경로 보안 `@_` 에서 순수한 상대 경로로 사용 `Dotiac::DTL` (장고 템플릿) 경로별 명령.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `parse_filename($path)`

</div>
  <div class="card-body">
    <div class="card-title">

주변 래퍼 `File::Basename::fileparse`. 인수가 없으면 `$_` 구문 분석할 파일 이름입니다.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `walk_content_tree($code)`

</div>
  <div class="card-body">
    <div class="card-title">

조건부로 걷기 `./content` 빌드 시스템 체크아웃의 트리; 먼저 정규화 `$_` 형식 컨텐트 루트 하위 경로로 변경한 다음 `$code->()` 트리워크의 각 항목에 대해 설명합니다.

</div>
<p class="text-body">

대부분의 빌드에서는 걷기가 발생하지 않습니다. &mdash; 대신 빌드는 이전 빌드의 캐시에 저장된 데이터를 사용합니다.

강제로 걷는 유일한 방법은 `$path::use_cache` 사용자가 제공한 모듈의 false 값 그렇지 않으면 이 동작은 [증분 빌드 기술](https://iconoclasts.blog/joe/dependencies).

캐시에 저장된 데이터에 의존하지 않고 실제로 보행시선이 진행된 경우 1을 반환합니다. 그렇지 않으면 false 값을 반환합니다.

</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

##### `archived($path)`

</div>
  <div class="card-body">
    <div class="card-title">

각 플래그 `Status: archived` `$path` (자연어 특정 방식으로). 사용 `$_` 인수가 전달되지 않은 경우

</div>
<p class="card-text">

마크다운 파일을 아카이브하는 것은 Orion에게 "이 파일의 고유 링크 위치에 대한 주의 중지"다시 업데이트되지 않으면 아카이브 위치가 새로 고쳐집니다. 특히 아카이브된 파일은 Orion CMS 자체 내의 디렉토리 목록에 나타나지 않습니다. 온라인으로 다시 편집할 수 있으려면 라이브 페이지 자체로 이동해야 합니다.

</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

##### `seed_file_deps($path)`

</div>
  <div class="card-body">
    <div class="card-title">

안전한 업데이트 `%path::dependencies` 대상 `$path`을 기반으로 `dependencies` 헤더 `glob(s)`.

</div>
<p class="card-text">

사용 기본값 `$_` 인수가 전달되지 않은 경우 경로로 지정됩니다.

</p>
</div>
</div>

<div class="card border-secondary mb-3">
  <div class="card-header">

##### `seed_file_acl($path)`

</div>
  <div class="card-body">
    <div class="card-title">

안전한 업데이트 `@path::acl` 대상 `$path`을 기반으로 `acl` 헤더 사양

</div>
<p class="card-text">

사용 기본값 `$_` 인수가 전달되지 않은 경우 경로로 지정됩니다.

</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `Load`

</div>
  <div class="card-body">
    <div class="card-title">

같음 `YAML::XS::Load`.

</div>
<p class="card-text">
</p>
</div>
</div>

<div class="card border-primary mb-3">
  <div class="card-header">

#### `Dump`

</div>
  <div class="card-body">
    <div class="card-title">

같음 `YAML::XS::Dump`.

</div>
<p class="card-text">
</p>
</div>
</div>
  </div>

<div class="tab-pane fade" id="stats" role="tabpanel">
    <script src="/editor.md/js/chart.umd.js"></script>
    <canvas id="myChart" width="100%" height="800px"></canvas>
</div>
</div>

<script async="" type="module">
  var ctx = document.getElementById("myChart").getContext("2d");
  const response = await fetch("/dynamic/search/?regex=build%3D;lang={{lang}};as_json=1;markdown_search=1");
  if (response.ok) {
    const json = await response.json();
	const data = json.duration;
    const values = data.map(x => x[1]);
    var total = 0;
    for (var i=0; i < values.length; ++i)
        total += +values[i];
    const labels = data.map(x => "r" + x[0] + ":" + x[2] + ":" + x[3] + ":" + x[4]);
    var myChart = new Chart(
      ctx,
      {
          type: "bar",
          data: {
              labels: labels.reverse(),
              datasets: [{
                  label: "Build Duration (s)",
                  data: values.reverse(),
                  backgroundColor: "#8f99fb",
              }],
          },
          options: {
              indexAxis: "y",
              plugins: {
                  title: {
                      display: true,
                      text: (total / 60).toFixed(0) + " build minutes this month (average build duration is " + (total / values.length).toFixed(0) + " s)",
                  }
              },
              onClick: (e, elts, chart) => {
                  if (elts) {
                      const idx = elts[0].index;
                      const revision = labels[idx].split(/:/)[0];
                      document.location = "/dynamic/search/?regex=build=" + revision + ";lang={{lang}};markdown_search=1";
                  }
              },
          },
      });
  }
</script>

<!-- $Date$ 저자 : 조 $ 개정 : 35135 $ -->
